---
outline: deep
---

## `/user`

User API for managing users. When you need to manage users, you can use the User API.

###

::: code-group

```
const main = async () => {
  const {
    inputFile,
    flags: { output, swaggerVersion },
  } = await runCli();
};
```

```js [index.js]
const main = async () => {
  const {
    inputFile,
    flags: { output, swaggerVersion },
  } = await runCli();
};
```

:::

# Tabelle

| Name     | Age | City          |
| -------- | --- | ------------- |
| John Doe | 25  | New York      |
| Jane Doe | 22  | San Francisco |
